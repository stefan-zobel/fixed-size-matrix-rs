initSidebarItems({"struct":[["HMatrix","A matrix which is allocated on the heap."],["MF","`MF` is the `M`atrix `F`actory used for the creation of new matrices."],["SMatrix","A matrix which is allocated on the stack. An `SMatrix` is itself Numeric, so that its elements can also be other stack-allocated matrices."]],"trait":[["Arithmetic","All types for which the operators `+`, `+=`, `*`, `*=`, `-` (unary negation), `-` (binary minus) and `-=` are defined and where the output type of `+=`, `*=` and `-=` is `T` again."],["Numeric","All types which are `Copy` and `Default` in addition to being Arithmetic."]]});